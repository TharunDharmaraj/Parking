<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:title="home"
        android:id="@+id/home"
        android:orderInCategory="100"
        app:showAsAction="always"
        android:icon="@drawable/home"/>
    <item android:title="poll"
        android:id="@+id/poll"
        android:orderInCategory="100"
        app:showAsAction="ifRoom"
        android:icon="@drawable/poll"/>
    <!-- action button for refresh -->
    <item android:title="refresh"
        android:id="@+id/refresh"
        android:orderInCategory="100"
        app:showAsAction="ifRoom"
        android:icon="@drawable/refresh"/>

    <!-- action button for copy -->
    <item android:title="about_us_menu"
            android:id="@+id/about_us_menu"
        android:orderInCategory="100"
        app:showAsAction="never"
        />
    <!-- action button for copy -->
    <item android:title="our_site_menu"
        android:id="@+id/our_site_menu"
        android:orderInCategory="100"
        app:showAsAction="never"
        />
    <!-- action button for copy -->
    <item android:title="@string/setDate"
        android:id="@+id/setRemainder"
        android:orderInCategory="100"
        app:showAsAction="never"
        />
    <!-- action button for copy -->
    <item android:title="@string/exit"
        android:id="@+id/exit"
        android:orderInCategory="100"
        app:showAsAction="never"
        />
</menu>
